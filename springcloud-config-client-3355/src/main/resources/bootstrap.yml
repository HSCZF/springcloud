# 系统级别的配置
spring:
  cloud:
    config:
      name: config-client     # 需要从git上读取的资源名称，不要后缀
      profile: test            # dev访问的是git上配置的config-client中的8201，test访问的是8202
      label: master
      uri: http://localhost:3344
